import { selectanzahl } from 'cssdesign/helpers/selectanzahl';
import { module, test } from 'qunit';

module('Unit | Helper | selectanzahl');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = selectanzahl([42]);
  assert.ok(result);
});
