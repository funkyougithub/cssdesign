/*
import Ember from 'ember';

export default Ember.Service.extend({

	flags: null,

	init() {
		this._super(...arguments);
		this.set('flags', []);
	},
	
	addFlag(item) {
		this.get('flags').pushObject(item);
	},
	
	readFlag() {
		return this.get('flags').length;
	},
	
	readTest() {
		return "TEST";
	},

	testvcdata: null
	
});
*/
